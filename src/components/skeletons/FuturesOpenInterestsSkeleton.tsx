/**
 * Bespoke skeleton for src/pages/app/futures/OpenInterests.tsx
 * Layout: PageTemplate shell only (page is a placeholder — title + empty body).
 */
import {
  SkeletonPage,
  SkeletonHeader,
} from '@/components/skeletons/shell';

export function FuturesOpenInterestsSkeletonPage() {
  return (
    <SkeletonPage>
      <SkeletonHeader titleWidth="w-40" withEyebrow={false} />
    </SkeletonPage>
  );
}
