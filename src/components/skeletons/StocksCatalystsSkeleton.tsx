/**
 * Bespoke skeleton for src/pages/app/stocks/Catalysts.tsx
 * Layout: PageTemplate shell only (page is a placeholder — title + empty body).
 */
import {
  SkeletonPage,
  SkeletonHeader,
} from '@/components/skeletons/shell';

export function StocksCatalystsSkeletonPage() {
  return (
    <SkeletonPage>
      <SkeletonHeader titleWidth="w-32" withEyebrow={false} />
    </SkeletonPage>
  );
}
