import { PageTemplate } from '@/components/PageTemplate';

export default function FundingBrokers() {
  return (
    <PageTemplate
      title="Brokers"
      description="Connect and manage broker accounts for funding."
    />
  );
}
