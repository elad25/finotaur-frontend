import { PageTemplate } from '@/components/PageTemplate';

export default function FundingTransactions() {
  return (
    <PageTemplate
      title="Transactions"
      description="View your deposit, withdrawal, and funding transaction history."
    />
  );
}
