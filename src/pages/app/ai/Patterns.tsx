import { PageTemplate } from '@/components/PageTemplate';

export default function AIPatterns() {
  return (
    <PageTemplate
      title="Pattern Detection"
      description="Automated technical pattern recognition across assets."
    />
  );
}
