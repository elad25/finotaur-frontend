import { PageTemplate } from '@/components/PageTemplate';

export default function ForexCorrelation() {
  return (
    <PageTemplate
      title="Correlation Map"
      description="Visualize correlations between currency pairs."
    />
  );
}
