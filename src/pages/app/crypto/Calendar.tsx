import { PageTemplate } from '@/components/PageTemplate';

export default function CryptoCalendar() {
  return (
    <PageTemplate
      title="Crypto Calendar"
      description="Track token unlocks, hard forks, and major crypto events."
    />
  );
}
