import { PageTemplate } from '@/components/PageTemplate';

export default function CryptoOnChain() {
  return (
    <PageTemplate
      title="On-Chain Data"
      description="Blockchain metrics, wallet activity, and network statistics."
    />
  );
}
