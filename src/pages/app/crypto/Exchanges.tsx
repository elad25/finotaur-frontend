import { PageTemplate } from '@/components/PageTemplate';

export default function CryptoExchanges() {
  return (
    <PageTemplate
      title="Exchanges"
      description="Compare volumes, liquidity, and spreads across exchanges."
    />
  );
}
