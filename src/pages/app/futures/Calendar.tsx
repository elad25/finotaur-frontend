import { PageTemplate } from '@/components/PageTemplate';

export default function FuturesCalendar() {
  return (
    <PageTemplate
      title="Futures Calendar"
      description="Contract expirations, rollovers, COT reports, and key commodity events."
    />
  );
}
