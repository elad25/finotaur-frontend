import { PageTemplate } from '@/components/PageTemplate';

export default function MacroEvents() {
  return (
    <PageTemplate
      title="Major Events"
      description="Elections, policy changes, and significant global events."
    />
  );
}
